/* ================================================================
   Deprecated location — the auth experience moved to
   src/components/auth/ (scene.tsx + layout.tsx). This file stays
   as a re-export so any older import path keeps compiling.
   ================================================================ */

export { AuthScene } from "@/components/auth/scene";
export { AuthSplit, AuthCardHeader, HairlineDivider } from "@/components/auth/layout";
